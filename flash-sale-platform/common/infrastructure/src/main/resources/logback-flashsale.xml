<!-- 공통 로깅 설정 프래그먼트. 각 서비스의 logback-spring.xml에서 include하여 사용 -->
<included>
    <!-- ========== 콘솔 출력 (개발용: 사람이 읽기 쉬운 형식) ========== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan(%-40.40logger{39}) : %msg %mdc%n</pattern>
        </encoder>
    </appender>

    <!-- ========== JSON 출력 (운영용: 구조화된 로그, 비동기 래핑) ========== -->
    <appender name="JSON_SYNC" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>requestId</includeMdcKeyName>
            <includeMdcKeyName>service</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <includeMdcKeyName>orderId</includeMdcKeyName>
            <includeMdcKeyName>productId</includeMdcKeyName>
            <shortenedLoggerNameLength>36</shortenedLoggerNameLength>
            <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSSZ</timestampPattern>
        </encoder>
    </appender>

    <!-- 비동기 래핑: 로깅이 비즈니스 스레드를 블록하지 않음 -->
    <appender name="JSON" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="JSON_SYNC"/>
        <!-- 큐가 80% 차면 TRACE/DEBUG 드롭 (운영 안정성) -->
        <discardingThreshold>20</discardingThreshold>
        <queueSize>1024</queueSize>
        <!-- MDC 값을 비동기 스레드로 전달 -->
        <includeCallerData>false</includeCallerData>
    </appender>

    <!-- ========== 파일 출력 (운영용: 컨테이너 외 환경 대비) ========== -->
    <appender name="FILE_SYNC" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/application.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>requestId</includeMdcKeyName>
            <includeMdcKeyName>service</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <shortenedLoggerNameLength>36</shortenedLoggerNameLength>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE_SYNC"/>
        <discardingThreshold>20</discardingThreshold>
        <queueSize>1024</queueSize>
    </appender>

    <!-- ========== 프레임워크 로그 레벨 ========== -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="io.lettuce" level="WARN"/>
    <logger name="org.redisson" level="WARN"/>
    <logger name="reactor" level="WARN"/>
    <logger name="io.netty" level="WARN"/>

    <!-- 비즈니스 로그는 DEBUG까지 허용 -->
    <logger name="com.flashsale" level="DEBUG"/>
</included>
